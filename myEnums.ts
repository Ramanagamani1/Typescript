enum SeatChoice {
    AISLE = 10,
    MIDDLE,
    WINDOW
}

const enum SeatAllotment {
    AISLE = "aisle",
    MIDDLE = "middle",
    WINDOW = 5,
    FOURTH
}

const seat = SeatChoice.WINDOW
const mySeat = SeatAllotment.AISLE
