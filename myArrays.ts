const arr = [1,2,3];
const superHeros: string[] = [];
const heroPowers: Array<number> = [];

type User = {
    name: string;
    isActive: boolean;
}

superHeros.push("spiderman")

const allUsers: Array<User> = [{name:"mani", isActive:true}]

const users: User[] = [];
users.push({name: "vaishu",isActive:true});

const mlModels: number[][] = [
    [123,456,789],
    [255,55,255]
]